# 01 — ארכיטקטורה

## העיקרון

**כל שכבה מקבלת קלט מוגדר ומחזירה פלט מוגדר. השכבה הבאה לא יודעת איך היא עשתה את זה.**

זה מה שקובע אם הוספת פיצ'ר בעוד שנה לוקחת שעה או יומיים.

---

## השכבות

```
        משתמש
          │
     ┌────┴────┐
     │ טלגרם   │
     └────┬────┘
          │
┌─────────┴──────────┐
│ 1  ingest.js       │  קליטה — לא מאבד, לא כופל
│ 2  router.js       │  ניתוב — מחליט מי מטפל
│ 3  extract.js      │  הבנה — טקסט/תמונה → מספרים
│ 4  rules.js        │  שיפוט — קוד בלבד
│ 5  coach.js        │  ניסוח — המודל
│ 6  memory.js       │  זיכרון — רצף
│ 7  proactive.js    │  יזום — פונה ראשון
└─────────┬──────────┘
          │
     ┌────┴────┐
     │  שיטס   │  ← האמת
     └─────────┘
```

---

## חוזי השכבות

חוזה = מה נכנס, מה יוצא, מה השכבה **לא** יודעת.

### 1. `ingest.js` — קליטה
```
IN   Telegram update (JSON)
OUT  { eventId, userId, type, payload, receivedAt }
```
- מחזיר HTTP 200 **מיד**, מעבד ברקע
- כותב ל-`raw_log` לפני כל עיבוד
- `update_id` = מפתח ייחודי → כפילות נדחית בשקט
- אלבום תמונות: צבירה 2 שניות לפי `media_group_id`
- דוחף לתור. **כותב אחד בכל רגע**

**לא יודע:** מה זה אימון, שיש מודל, שיש שיטס

---

### 2. `router.js` — ניתוב
```
IN   event
OUT  { handler: "query"|"update"|"extract"|"chat", params }
```
**זו השכבה שהופכת את המערכת לזולה.** רוב ההודעות לא צריכות מודל.

| סוג | דוגמה | עלות |
|---|---|---|
| `query` | "כמה ימים מאז ריצה" | 0 טוקנים, מיידי |
| `update` | "בוצע" | 0 טוקנים |
| `extract` | תמונות | Haiku |
| `chat` | הכל השאר | Sonnet |

ניתוב לפי התאמת תבניות. ספק → `chat`.

**לא יודע:** תוכן הכללים, מה הפסק יהיה

---

### 3. `extract.js` — הבנה
```
IN   טקסט חופשי או 1-4 תמונות
OUT  { fields: {...}, confidence, missing: [...] }
```
- תמונות → base64 → Claude → JSON מובנה
- **ולידציה מספרית לפני החזרה** (ראה `06_FAILURE_MODES`)
- שדה שלא זוהה → `missing`, לעולם לא ניחוש
- **חובה: אישור משתמש לפני כתיבה**

**לא יודע:** אם זה מותר, מה ההמלצה

---

### 4. `rules.js` — שיפוט ← הלב
```
IN   { state, workout, counters }
OUT  { verdict: "allow"|"block", ruleId, reason, severity }
```
**קוד בלבד. אפס AI. לא ניתן לשכנוע.**

מריץ את `rules.json` בסדר עדיפות. עוצר בכלל החוסם הראשון.

**לא יודע:** איך לנסח, שיש טלגרם

---

### 5. `coach.js` — ניסוח
```
IN   { verdict, state, memory, userMessage }
OUT  טקסט תשובה
```
מנסח את הפסק כמאמן. **מנסח, לא מחליט.**

הפסק מוזרק לפרומפט כעובדה נתונה, לא כשאלה.

**לא יודע:** איך הפסק התקבל, איך לכתוב לשיטס

---

### 6. `memory.js` — זיכרון
```
IN   userId, contextDepth
OUT  { conversation, session, preferences, state }
```
ראה `04_MEMORY.md`

---

### 7. `proactive.js` — יזום
```
IN   cron tick
OUT  הודעות יוצאות
```
טריגרים: בוקר אחרי אימון · מונה חורג · סיכום שבועי · בדיקת חיים

---

### `sheets.js` — גישה לנתונים
```
read(tab, range) / write(tab, row) / update(tab, rowId, fields)
```
**השכבה היחידה שנוגעת בשיטס.** קריאת עמודות **לפי שם, לא לפי מיקום**.

---

## זרימות

**דיווח אימון**
```
תמונות → ingest → router(extract) → extract
       → אישור משתמש → sheets.write
       → rules → coach → תשובה
```

**שאילתה**
```
"כמה ימים מאז ריצה" → ingest → router(query)
                    → state → תשובה   [0 טוקנים]
```

**בוקר אחרי אימון**
```
cron → proactive → "איך הברך, המפשעה, האכילס?"
```

**חקירה שבועית**
```
cron(שבת 02:00) → קריאת שבוע → Opus
  → סריקת דוגמאות-נגד (קוד, כל ההיסטוריה)
  → מנתח שני (נתונים גולמיים בלבד, בלי המסקנה)
  → הצעה למשתמש → אישור → rules.json
```

---

## איפה יושב מה

| | תוכן |
|---|---|
| **Google Sheets** | האמת. אימונים, משקל, כללים, המלצות, יומן שיחות |
| **SQLite מקומי** | לוג גולמי, תור, שיחות, מטמון |
| **קבצים** | `rules.json`, קונפיג |

**כלל:** מטמון ↔ שיטס נחלקים → **המטמון טועה, תמיד.** פקודת `rebuild` בונה מחדש.

---

## הוספת פיצ'רים

| רוצה | נגע ב |
|---|---|
| כלל חדש | `rules.json` — שורה. אפס קוד |
| תזכורת | `proactive.js` |
| שאילתה | `router.js` |
| ערוץ (וואטסאפ) | `ingest.js` בלבד |
| מודל אחר | `coach.js` בלבד |

---

## מה יהרוס את זה

1. **המודל מחליט משהו שהקוד היה צריך להחליט** → הבטיחות הופכת להמלצה
2. **שכבה שיודעת יותר מדי** → `coach.js` שקורא ישירות מהשיטס שובר את ההפרדה
3. **קובץ מעל 300 שורות** → תיקון יקרא אותו כולו, וזה מייקר כל סשן
