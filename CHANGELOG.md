# CHANGELOG

**שינויי התנהגות בלבד.** מה שהמשתמש היה מרגיש. שינויי קוד פנימיים → `BUILD_LOG.md`.

---

## כללי שימוש

- רושמים אחרי שהשינוי **עובד ונבדק**, לא כשתוכנן
- ממתין לאישור → `Pending`
- אין לשחזר כלל ישן בלי לרשום שהוא מחליף החלטה מאוחרת יותר
- סתירה בין קובץ פעיל ל-CHANGELOG → **לא מכריעים לפי CHANGELOG בלבד.** עוצרים ושואלים

---

## קטגוריות

`Added` · `Changed` · `Fixed` · `Rules` · `Removed`

---

## [Unreleased]

### Added
- חבילת מפרט (2026-08-16)
- שלד מקומי לשלב 1: webhook תואם Telegram, לוג גולמי עמיד, תור סדרתי,
  מניעת כפילויות ואיחוד אלבומי תמונות.
- מתאם Sheets לקריאה בלבד מנתוני דמה מקומיים.
- שש בדיקות אוטומטיות לתרחישי הקליטה, ההתאוששות וגבול הכתיבה.
- מנוע שלב 2 מקומי: ניתוב ללא מודל, כללי hard block לפי עדיפות, מונים
  מפעילות שהושלמה, `rebuild` דטרמיניסטי ותקרת טוקנים קשיחה.
- שאילתת "כמה ימים מאז ריצה" נענית מיד עם אפס שימוש בטוקנים.
- אחת־עשרה בדיקות שלב 2, כולל נתוני יום־אחרי חסרים, ניסיונות עקיפה עוינים,
  שחזור חוזר ומניעת פעולה מודלית לאחר מיצוי התקציב.
- מאמן שלב 3 עם Claude Sonnet, זיכרון שיחה וסשן עמיד, חיפוש "מה אמרנו על X"
  ומודעות לפער של שלושה ימים ומעלה.
- חילוץ 1–4 צילומי מסך עם Claude Haiku, ולידציה של תאריך, דופק, מרחק, קצב,
  זונים, RPE וכאב, ושמירת נתון חסר כריק במקום ניחוש.
- שער אישור עמיד לחילוץ: כתיבה מתבצעת רק מ־`confirm_extraction:<id>` מפורש;
  אישור כפול, אישור חילוץ שגוי והודעת "בוצע" רגילה אינם כותבים.
- prompt caching אוטומטי בחיבור Claude והורדת תמונות Telegram ללא dependency חדש.
- תשובות שלב 3 נשלחות ל־Telegram דרך outbox עמיד; חילוץ מציג כפתורי אישור
  וביטול, וכשל שליחה אינו יוצר תשובה, זיכרון או חילוץ כפולים.
- עשרים ואחת בדיקות שלב 3; הסוויטה המלאה כוללת 40 בדיקות.

### Changed
- שלב 0 הושלם: כותרות היום־אחרי נוספו, שדות היום־אחרי מולאו לפי האישור,
  ותצוגת תאריכי Weight Tracker נורמלה ל־`YYYY-MM-DD`.
- שלב 2 נכשל כעת סגור כאשר סיווג שינוי העומס חסר או לא חוקי.
- `rebuild` והמונים דורשים תאריך `asOf` מפורש, ו־`npm start` מחובר למעבד
  שלב 2 המקומי ללא ביצוע קריאת מודל.
- `npm start` מחובר כעת למעבד שלב 3. שיחת העלאת עומס מסווגת לפני המודל,
  עוברת דרך `rules.js`, וב־hard block אינה מפעילה שום קריאת מודל.
- תקרת המודל מחושבת כעת לפי קלט הבקשה המלא בפועל ועוד הפלט המרבי לפני יצירת
  תשובה, במקום reservation קבוע לפי סוג route.
- תקרת המודל נשמרת לפי יום UTC גם לאחר הפעלה מחדש. כשהיא נגמרת המשתמש מקבל
  הודעה ברורה, בעוד ששאילתות מקומיות ממשיכות לפעול.
- תשובות ארוכות מפוצלות אוטומטית למספר הודעות Telegram במקום להיכשל.

### Fixed
- placeholders ושגיאות נתונים שאושרו לניקוי הוסרו מ־Runs, Splits ו־Goals.
- Goal Time רוקן בשלושה אימוני כוח שבהם המדד אינו ישים.
- תמונת אלבום שמגיעה לאחר שהאלבום כבר נתפס לעיבוד אינה נעלמת עוד; היא
  נשמרת כפריט תור עצמאי המסומן כ־late arrival.
- הפעלה ללא `WEBHOOK_SECRET` מדפיסה כעת אזהרת אבטחה בולטת לפני פתיחת השרת.
- פריט תור שקרס בניסיון האחרון אינו חוזר לעיבוד לאחר הפעלה מחדש ואינו יכול
  לעבור את תקרת `maxAttempts`.
- callbacks בצורת Telegram queue אמיתית מזוהים, ובקשת תכנון כמו "מה כדאי
  לעשות מחר?" אינה עוקפת עוד את מנוע הכללים בשל היעדר מילת אימון.
- ניסוחי תכנון נוספים כגון "תבנה לי תוכנית למחר" ו־"תן לי ריצה למחר"
  עוברים כעת דרך מנוע הכללים.
- תוצאת כתיבה לא ודאית ל־Sheets אינה נשלחת שוב אוטומטית, כדי למנוע שורת
  אימון כפולה; סביבת הדמו מודיעה במפורש שהכתיבה מושבתת.

---

## תבנית

```markdown
## [גרסה] — תאריך

### Rules
- כלל `id` נוסף/שונה. אושר ע"י ליאור. בסיס: [ראיות]

### Changed
- ...
```
