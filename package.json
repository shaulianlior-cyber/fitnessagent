{
  "name": "running-coach-agent-stage-3",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stage 3 local running coach with durable memory, validated screenshot extraction, and explicit write confirmation.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24"
  }
}
