# 05 — תוכנית בנייה

## כללי עבודה

- **שלב אחד לסשן.** סשן ארוך יקר יותר מארבעה ממוקדים.
- **קובץ אחד לשכבה. מקסימום 300 שורות.** תיקון קורא קובץ, לא פרויקט.
- **לא עוברים שלב בלי קריטריון יציאה.**
- **מעדכנים `BUILD_LOG.md` בסוף כל סשן.**

---

## חייב להיות נכון מהיום הראשון

להוסיף אחר כך = לגעת בכל שכבה:

1. **לוג גולמי** append-only
2. **תור סדרתי** — כותב אחד בכל רגע
3. **תלת-מצב** — `clean` / `issue` / ריק
4. **הפרדת מנוע-כללים**
5. **UTC פנימית**

**כל השאר אפשר להוסיף בשקט אחר כך.**

---

## שלב 0 — לפני קוד ⚠️

ידני. ~3 שעות.

- [ ] ניקוי 8 שגיאות השיטס (`02_SCHEMA.md`)
- [ ] הוספת העמודות החדשות
- [ ] הכרעה מתועדת: **השיטס הוא האמת**
- [ ] `DECISIONS.md` מעודכן

**אסור להתחיל קוד לפני שזה סגור.** כלי AI יבנה לוגיקה סביב כל באג שיישאר.

---

## שלב 1 — שלד

**בלי AI בכלל. נתוני דמה.**

`ingest.js` · `queue` · `raw_log` · `sheets.js` · webhook

**יציאה:**
- הודעה נשלחת → נכתבת לשיטס
- אותה הודעה פעמיים → נדחית בשקט
- 4 תמונות באלבום → אירוע אחד
- הבוט נופל ועולה → התור ממשיך מאיפה שהפסיק

---

## שלב 2 — מנוע

`router.js` · `rules.js` · `rules.json` · מונים · `rebuild` · תקרת תקציב

**יציאה:**
- "כמה ימים מאז ריצה" → תשובה מיידית, **0 טוקנים**
- יום-אחרי ריק → חסימה
- **ניסיון לשכנע את הבוט לעקוף → נכשל**
- `rebuild` → מצב זהה לשיטס

---

## שלב 3 — המאמן

`coach.js` · `memory.js` · `extract.js` + תמונות · prompt caching

**יציאה:**
- 4 צילומי מסך → שורה מדויקת, בלי הקלדה
- אישור לפני כתיבה — **תמיד**
- שיחה נמשכת בין ימים
- "מה אמרנו על X" → נמצא

---

## שלב 4 — יזום

`proactive.js` · cron · בדיקת חיים

**יציאה:**
- בוקר אחרי אימון → **הבוט שואל ראשון**
- מונה חורג → התרעה
- הבוט נופל → הודעה למשתמש

**זה הרכיב עם התמורה הגבוהה ביותר בכל המערכת.**

---

## שלב 5 — למידה + הלבשת הדומיין

חקירה שבועית · סריקת דוגמאות-נגד · מנתח שני · לולאת תוצאות
**+ טעינת הכללים והדפוסים מהמערכת הקיימת ל-`rules.json`**

**יציאה:**
- חקירה מציעה דפוס עם ראיות + ביטחון + דוגמאות-נגד
- דפוס עם 3 דוגמאות-נגד → נחסם אוטומטית
- **הבוט לא משנה כלל בעצמו**

---

## מה שיישבר בהתחלה

**OCR על מסכי שעון.** תמונה מטושטשת, פורמט חדש. לכן שכבת האישור — היא לא זהירות יתר, היא הכרח.

**הכיול ייקח יותר זמן מהקוד.** כמה קצר, מה נדחס, מתי לפנות ומתי זה מציק. ~שבועיים-שלושה של תיקונים קטנים. זו לא עבודת קוד — זו עבודת שימוש.

---

## עלות

| | |
|---|---|
| VPS | $5 |
| מודלים | $5–15 |
| **סה"כ** | **~$10–20/חודש** |

החקירה השבועית היא הפריט היקר. דו-שבועית = חצי.
