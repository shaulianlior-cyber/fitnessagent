{
  "name": "running-coach-agent-stage-2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stage 2 local routing and deterministic rules engine with fixture-backed, read-only Sheets data.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24"
  }
}
