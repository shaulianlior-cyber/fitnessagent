# יומן בנייה

**מתעדכן בסוף כל סשן. בלי זה הסשן הבא מתחיל מאפס.**

---

## תבנית

```markdown
## [תאריך] — שלב X — [כלי]

**נבנה:**
- קובץ/פונקציה — מה עושה

**נבדק:**
- [ ] קריטריון יציאה 1
- [ ] קריטריון יציאה 2

**נשבר / לא עובד:**
- מה, ומה נוסה

**פתוח לסשן הבא:**
- הדבר הראשון שצריך לעשות

**החלטות:** → DECISIONS.md #NNN (אם היו)
**עלות סשן:** ~$X
```

---

## 2026-08-17 — שער בטיחות לפני שלב 4 — Codex

**תוקן:**

- `router.js` מזהה כעת באופן קטגוריאלי בקשת החלטה, תכנון עתידי או שינוי
  עומס ומעביר אותה ל־rules. דיווח עבר או הווה, שאלת ידע, תחושה, ניתוח
  ושיחת חולין נשארים אינפורמטיביים; אין allowlist של משפטי שיחה.
- זיהוי העלאת עומס הורחב גם לגוף ראשון רבים ולרצף החלקיקים `לי את`, אך
  הבטיחות אינה תלויה בהרחבת ה־regex.
- `rules.json` חוסם בעדיפות 5 ערך יום־אחרי קיים שאינו `clean` או `issue`;
  חסר נשאר מצב נפרד וממשיך להיחסם בכלל הייעודי.
- `AGENTS.md` עודכן ממצב שלב 2 הישן למצב בפועל: שלבים 1–3 הושלמו והעבודה
  הנוכחית היא שער בטיחות בלבד לפני שלב 4.

**נבדק:**

- [x] בדיקות הרגרסיה החדשות נכשלו תחילה על הקוד הקודם: בקשות חופשיות הגיעו
  למודל ו־`probably fine` קיבל `allow`.
- [x] ארבעת ניסוחי העומס שנמסרו נחסמים ללא קריאת מודל.
- [x] 14 הודעות מגוונות של דיווח אימון, תסמין, מדדים, שקילה, שאלת ידע,
  ניתוח, הרגל, ברכה ותודה ממשיכות להגיע למודל.
- [x] ערך תלת־מצב מחוץ לאוצר המילים מייצר hard block.
- [x] `npm test`: 43 עברו, 0 נכשלו, כולל כל 40 הרגרסיות הקודמות.

**ביקורת חוזרת:** לאחר פרסום ה־PR התגלה ש־allowlist ראשוני של שלושה regexים
חסם כמעט כל שיחה רגילה. בדיקת הקטגוריות החדשה נכשלה תחילה על `בוקר טוב`,
והמימוש הוחלף בסיווג כוונה קטגוריאלי לפני האימות המלא.

**נשבר / לא עובד:** אין כשל פתוח ידוע. לא בוצעו חיבור או כתיבה ל־Google
Sheets, ולא נגעו ב־Stage 4, `proactive`, cron או deployment.

**פתוח לסשן הבא:** ביקורת ומיזוג ה־PR הממוקד. אין להתחיל שלב 4 לפני בקשה
מפורשת נפרדת.

**החלטות:** לא התקבלה החלטת ארכיטקטורה חדשה; התיקון אוכף את ADR 002, 004
ו־005 ולכן `DECISIONS.md` לא השתנה.

---

## 2026-08-16 — שלב 3 — שער מיזוג סופי — Codex

**אומת:**

- `npm test`: 40 עברו, 0 נכשלו, כולל כל רגרסיות שלבים 1 ו־2.
- `git diff --check` נקי, כל קובצי המקור עד 300 שורות ואין `.env`, קובץ
  SQLite, credential או secret במעקב Git.
- כל חמשת שרשורי הביקורת פתורים; GitHub מדווח על PR מספר 4 כ־`CLEAN`
  ו־`MERGEABLE`. אין GitHub checks מוגדרים במאגר.
- ה־head המקומי והמרוחק זהים, ועץ העבודה נקי.
- המשתמש אישר במפורש לסגור את שלב 3 ולמזג את PR מספר 4 אם הוא נקי.

**מסירה:** שלב 3 מאושר למיזוג באמצעות merge commit, כדי לשמור את היסטוריית
התיקונים ולאפשר revert מרוכז. ה־PR יסומן ready וימוזג לאחר commit יומן זה.

**פתוח לסשן הבא:** ביקורת עצמאית של Claude על `main` הממוזג. אין להתחיל
שלב 4 בלי בקשה מפורשת נפרדת.

**החלטות:** לא התקבלה החלטת ארכיטקטורה חדשה; `DECISIONS.md` לא השתנה.

---

## 2026-08-16 — שלב 3 — תיקוני ביקורת חוזרת PR 4 — Codex

**תוקן:**

- ניתוב fail-closed מכסה כעת ניסוחי תכנון טבעיים כגון "תבנה לי תוכנית
  למחר", "איזה אימון לעשות מחר?" ו־"תן לי ריצה למחר"; הוספת קילומטרים
  מזוהה במפורש כהעלאת עומס.
- תקציב המודל נשמר ב־SQLite לפי יום UTC, שורד הפעלה מחדש ומתאפס ביום הבא.
  כשאין תקציב, coach/extract אינם יוצרים תשובה מודלית והמשתמש מקבל הודעה
  ששאילתות מקומיות עדיין זמינות.
- תשובות ארוכות מפוצלות להודעות Telegram של עד 4,096 תווים עם outbox נפרד
  לכל חלק; עותק הזיכרון נחתך בבטחה ל־8,000 תווים בלי להפיל את החיפוש.
- לפני כתיבת Sheets נשמר latch עמיד. שגיאה בעלת תוצאה לא ידועה נועלת retry
  אוטומטי ומחזירה הודעה למשתמש; שגיאת הדמו הידועה כ־`not_written` משחררת את
  הנעילה ומדווחת שהכתיבה מושבתת.
- `sheets.write` מקבל מפתח idempotency יציב לקראת מתאם Sheets חי עתידי.

**נבדק:**

- [x] ארבעת ניסוחי התכנון שנכשלו בביקורת עוברים ל־`workout` ונחסמים ללא מודל.
- [x] התקציב נשמר בין שני מופעי runtime, חוסם לפני Count Tokens כשכבר אין
  מקום לפלט המרבי, ומתאפס בחצות UTC.
- [x] חיפוש של חמש הודעות ארוכות אינו קורס וכל חלק שנשלח עומד במגבלת Telegram.
- [x] כשל Sheets עמום ואישור חוזר מבצעים ניסיון כתיבה אחד בלבד.
- [x] מצב הדמו מחזיר הודעת `write_unavailable` ומשאיר את החילוץ ללא כתיבה.
- [x] `npm test`: 40 עברו, 0 נכשלו, כולל כל רגרסיות שלבים 1 ו־2.

**PR:** תיקון `4003d53` נדחף ל־PR מספר 4 ותיאור ה־PR עודכן ל־40 בדיקות
ולארבעת תיקוני הביקורת החוזרת.

**פתוח לסשן הבא:** לבצע ביקורת חוזרת על ה־head החדש. אין להוציא את ה־PR
מ־draft ואין להתחיל שלב 4 לפני שהביקורת נקייה.

**החלטות:** → DECISIONS.md #015.

---

## 2026-08-16 — שלב 3 — ביקורת חוזרת PR 4 — Codex

**נבדק:**

- ה־head `9b8f6b6` מול `main`, כל קוד שלב 3, 35 הבדיקות והחוזים שבמפרט.
- כל חמשת שרשורי הביקורת הקודמים פתורים ואין GitHub checks מוגדרים.
- `npm test`: 35 עברו, 0 נכשלו; `git diff --check` נקי.
- הורצו תרחישי קצה נוספים שלא הופיעו בסוויטה.

**ממצאים:**

- P1 — ניתוב ההמלצות עדיין אינו fail-closed לכל ניסוח טבעי. לדוגמה
  "תבנה לי תוכנית למחר", "איזה אימון לעשות מחר?" ו־"תן לי ריצה למחר"
  מגיעים כ־`informational` בלי `workout`, ולכן אינם עוברים דרך `rules.js`.
- P1 — תקרת המודל היא לכל חיי התהליך, אינה תקרה יומית עמידה, וחריגה במסלול
  coach/extract נכשלת בתור בלי הודעה למשתמש. זה סותר את חוזה הכשל המתועד:
  חריגה מודיעה, מפסיקה מודל ומשאירה שאילתות מקומיות פעילות.
- P1 — חיפוש שיחה יכול לחבר עד חמש הודעות ארוכות. תוצאה מעל 8,000 תווים
  נזרקת בזמן שמירת תשובת העוזר; גם תוצאה קצרה יותר עלולה לעבור את מגבלת
  4,096 התווים של Telegram. התרחיש שוחזר מקומית.
- P1 — אישור חילוץ כותב ל־Sheets ורק אחר כך מסמן את הרשומה `confirmed`.
  כשל רשת עמום אחרי שהכתיבה התקבלה משאיר `pending`; retry כותב אותה שורה
  שוב. התרחיש שוחזר עם שתי כתיבות מאישור יחיד.

**שינויים בקוד:** לא בוצעו; זו ביקורת בלבד. `BUILD_LOG.md` עודכן מקומית.

**פתוח לסשן הבא:** לתקן את ארבעת הממצאים בתוך שלב 3, להוסיף בדיקות ייעודיות,
להריץ את כל שערי האימות ורק אז לבצע ביקורת חוזרת נוספת. PR מספר 4 אינו מוכן
לצאת מ־draft. אין להתחיל שלב 4.

**החלטות:** לא התקבלה החלטת ארכיטקטורה חדשה בביקורת; `DECISIONS.md` לא השתנה.

---

## 2026-08-16 — שלב 3 — תיקוני ביקורת PR 4 — Codex

**תוקן:**

- callback במבנה האמיתי של פריט התור קורא כעת
  `event.update.callback_query.data` ומגיע לאישור או לביטול החילוץ.
- מעבד שלב 3 שומר תוצאה ו־outbox עמידים, שולח תשובות וכפתורי אישור ל־Telegram,
  ו־retry של כשל שליחה אינו מפעיל שוב מודל או מוסיף זיכרון כפול.
- כל בקשת Claude נספרת מראש דרך Count Tokens; התקציב שומר קלט אמיתי ועוד פלט
  מרבי לפני יצירת תשובה, כולל בקשות עם תמונות.
- בקשות תכנון תלויות־זמן כגון "מה כדאי לעשות מחר?" נכשלות סגור ועוברות דרך
  `rules.js` גם ללא מילת אימון מפורשת.
- זיכרון וחילוצים קיבלו מפתחות מקור ייחודיים, כך שאותו פריט תור אינו יוצר
  שיחות או pending extraction כפולים.

**נבדק:**

- [x] callback אמיתי בצורת queue מגיע ל־`confirm_extraction`.
- [x] כשל Telegram ראשון והצלחה ב־retry מבצעים שתי שליחות אך רק שתי קריאות
  מודל מקוריות — מאמן ומסכם — ושומרים שתי הודעות שיחה בלבד.
- [x] אותו מפתח חילוץ פעמיים מפעיל extractor פעם אחת ויוצר שורה ממתינה אחת.
- [x] קלט של 8,000 טוקנים עם פלט מרבי 1,024 נחסם לפני `generate` תחת תקרה 8,192.
- [x] "מה כדאי לעשות מחר?" נחסם דטרמיניסטית בלי קריאת מודל.
- [x] `npm test`: 35 עברו, 0 נכשלו, כולל כל רגרסיות שלבים 1 ו־2.
- [x] `git diff --check` נקי וכל קובצי המקור נשארו עד 300 שורות.
- [x] לא נוסף dependency, סוד, SQLite runtime או חיבור Google Sheets חי.

**נשבר / לא עובד:** כשל Telegram מדומה נכשל בניסיון הראשון בכוונה; ה־retry
השתמש ב־outbox והצליח ללא עיבוד מודלי חוזר. אין כשל פתוח ידוע בתיקונים.

**PR:** תיקון `6e07832` נדחף ל־PR מספר 4 ותיאור ה־PR עודכן. לאחר הרשאה
מפורשת, כל חמשת שרשורי הביקורת סומנו כפתורים ונקראו מחדש במצב
`isResolved: true`.

**פתוח לסשן הבא:** לבצע ביקורת חוזרת על ה־head החדש ולקבוע אם PR מספר 4
מוכן לצאת מ־draft. אין להתחיל שלב 4.

**החלטות:** → DECISIONS.md #013–#014.

---

## סטטוס נוכחי

| שלב | מצב |
|---|---|
| 0 — ניקוי נתונים | ✅ הושלם |
| 1 — שלד | ✅ הושלם מקומית על נתוני דמה |
| 2 — מנוע | ✅ הושלם ומוזג |
| 3 — מאמן | ✅ הושלם ומוזג |
| 4 — יזום | ⬜ |
| 5 — למידה + הלבשה | ⬜ |

---

## 2026-08-16 — שלב 3 — ביקורת קוד PR 4 — Codex

**נבדק:**

- PR מספר 4 פתוח כ־draft, ניתן למיזוג ומצביע ל־commit `7166611` מול `main`.
- אין כרגע GitHub checks, ביקורות קוד קודמות או שרשורי review אחרים.
- ה־diff המפורסם נבדק מול חוזי הבטיחות, התור, התקציב וקריטריוני היציאה
  של שלב 3.
- פורסמה ביקורת COMMENT עם חמישה שרשורים inline.

**ממצאים:**

- P1 — callback אמיתי בצורת ה־queue אינו קורא את
  `event.update.callback_query.data`, ולכן אישור חילוץ אינו מגיע לנתיב האישור.
- P1 — מעבד שלב 3 רק רושם ומחזיר תשובה; התור משליך את הערך ואין שליחה חזרה
  ל־Telegram או כפתורי אישור למשתמש.
- P1 — reservations קבועים אינם תקרה קשיחה לקלט אמיתי של ארבע תמונות או
  היסטוריית שיחה ארוכה; usage בפועל מדווח אך אינו נאכף.
- P1 — "מה כדאי לעשות מחר?" אינו מכיל מילת אימון ולכן מגיע למודל כ־
  `informational` בלי פסק של `rules.js`.
- P2 — retry אינו idempotent: אין מפתח אירוע בזיכרון או בחילוץ, ולכן כשל או
  קריסה יכולים להכפיל הודעות, קריאות מודל וחילוצים ממתינים.

**שינויים בקוד:** לא בוצעו; זו הייתה ביקורת בלבד.

**פתוח לסשן הבא:** לתקן את חמשת הממצאים בתוך שלב 3, להוסיף בדיקות runtime
לצורת queue/callback/retry, להריץ את כל השערים ולעדכן את PR מספר 4. אין להתחיל
שלב 4.

**החלטות:** לא התקבלה החלטת ארכיטקטורה חדשה; `DECISIONS.md` לא השתנה.

---

## 2026-08-16 — שלב 3 — מאמן — Codex

**נבנה:**

- `coach.js` — מנסח פסק קיים בעברית ישירה; hard block מוחזר בקוד ואינו
  מגיע למודל, גם תחת ניסיונות שכנוע.
- `memory.js` — שיחה, סיכום סשן, פריטים פתוחים והעדפות מאושרות נשמרים
  ב־SQLite; פער של 3+ ימים מוצג וחיפוש טקסטואלי מחזיר עד חמישה קטעים.
- `extract.js` + `telegram_media.js` — מורידים 1–4 תמונות, מחלצים שורת אימון,
  שומרים חסר כריק ומאמתים תאריך, טווחים, קצב וסכום זונים.
- `pending_extractions` — חילוץ נשמר לפני כתיבה; רק callback מפורש עם המזהה
  מאשר, שגיאת ולידציה חוסמת ואישור כפול אינו כותב שוב.
- `model.js` — חיבור Messages API ללא dependency, עם Haiku לחילוץ/סיכום,
  Sonnet למאמן ו־prompt caching אוטומטי.
- runtime של שלב 3 — משתמש עדיין ב־fixture ובמתאם Sheets לקריאה בלבד; אין
  חיבור או כתיבה ל־Google Sheets האמיתי.

**נבדק:**

- [x] ארבעה צילומי מסך יוצרים שורה אחת מדויקת וממתינה לאישור.
- [x] "בוצע", חילוץ שגוי ואישור כפול אינם כותבים; callback מפורש כותב פעם אחת.
- [x] שיחה, סשן והעדפה מאושרת נשמרים בין ימים; "מה אמרנו על X" מוצא היסטוריה.
- [x] חמישה ניסיונות שכנוע/המלצה להעלות עומס עם נתונים חסרים נעצרים ב־rules.
- [x] payload של Claude כולל caching וחשבונאות usage, ללא קריאת רשת בבדיקות.
- [x] `npm test`: 27 עברו, 0 נכשלו, כולל כל רגרסיות שלבים 1 ו־2.

**נשבר / לא עובד:**

- בדיקת חיפוש ציפתה תחילה להתאמה מורפולוגית בין "נעליים" ל־"נעלי"; הציפייה
  תוקנה לחיפוש מילולי בהתאם למגבלה המתועדת.
- לא בוצעה קריאת Claude חיה כי לא סופק מפתח ולא נדרש להוציא עלות; החיבור נבדק
  מול תגובת HTTP מזויפת. כתיבה חיה ל־Sheets נשארה חסומה במכוון.
- `gh` לא נמצא תחילה ב־PATH של Codex, אך אותר ב־`C:\Program Files\GitHub CLI`
  וההתחברות לחשבון GitHub אומתה דרך הנתיב המלא.

**פתוח לסשן הבא:** ביקורת חיצונית על draft PR של שלב 3. אין להתחיל שלב 4.

**החלטות:** לא התקבלה החלטת ארכיטקטורה חדשה; המימוש מיישם את ADR 002, 004,
005, 008, 009 ו־010 ולכן `DECISIONS.md` לא השתנה.

---

## 2026-08-16 — שער לפני שלב 3 — התאוששות `maxAttempts` — Codex

**תוקן:**

- התאוששות פריטי תור שנקטעו עברה מ־`db.js` אל `PersistentQueue`, שמכיר את
  תקרת הניסיונות שהוגדרה בקוד.
- פריט שנקטע לפני התקרה חוזר ל־`pending`; פריט שכבר הגיע לתקרה מסומן
  `failed` ואינו מעובד שוב לאחר הפעלה מחדש.
- גם מצב `pending` ישן שכבר הגיע לתקרה נסגר כ־`failed`, ו־`claimNext` אינו
  יכול לתפוס פריט שמיצה את מספר הניסיונות.
- נוסף אימות ש־`maxAttempts` הוא מספר שלם חיובי ובטוח.

**נבדק:**

- [x] שחזור רגיל לפני התקרה עדיין מעבד את הפריט לאחר הפעלה מחדש.
- [x] קריסה בניסיון השלישי אינה גורמת לניסיון רביעי.
- [x] מצב ישן שבו פריט שמיצה ניסיונות נשאר `pending` נסגר בבטחה.
- [x] `npm test`: 19 עברו, 0 נכשלו, כולל כל בדיקות שלבים 1 ו־2.
- [x] `git diff --check` נקי וכל קובצי המקור נשארו עד 300 שורות.
- [x] לא נוסף חיבור ל־Sheets, מודל, סוד או dependency חדש.

**נשבר / לא עובד:** אין כשל פתוח ידוע בשער זה.

**פתוח לסשן הבא:** לאחר מיזוג ה־PR הממוקד, אפשר להתחיל את שלב 3 רק לפי
בקשה מפורשת ובסשן נפרד.

**להעברה הבאה לקלוד:** לכלול בביקורת גם את שער `maxAttempts`, ובפרט את
בדיקת הרגרסיה שמדמה קריסה בניסיון האחרון ומוודאת שאין ניסיון נוסף.

**החלטות:** לא התקבלה החלטת ארכיטקטורה חדשה; התיקון אוכף את חוזה הניסיונות
הקיים ולכן `DECISIONS.md` לא השתנה.

---

## 2026-08-16 — שלב 2 — שער מיזוג סופי — Codex

**אומת:**

- GitHub מדווח ש־PR מספר 2 הוא `CLEAN` ו־`MERGEABLE`, וה־head המקומי זהה
  ל־head המרוחק.
- כל שלושת שרשורי הביקורת פתורים וכל ארבעת הממצאים תוקנו.
- `npm test`: 18 עברו, 0 נכשלו; `git diff --check` נקי.
- המשתמש אישר במפורש למזג אם המצב בטוח וקל לתיקון עתידי.

**מסירה:** PR מספר 2 מאושר למיזוג באמצעות merge commit, כדי לשמור את מלוא
ההיסטוריה ולאפשר revert יחיד אם תימצא טעות מאוחר יותר.

**פתוח לסשן הבא:** לטפל רק בשער poison-pill / `maxAttempts` לפני שלב 3.

**החלטות:** לא התקבלה החלטת ארכיטקטורה חדשה; `DECISIONS.md` לא השתנה.

---

## 2026-08-16 — שלב 2 — תיקוני ביקורת PR 2 — Codex

**תוקן:**

- `src/stage2.js` אינו מקבל או מפעיל עוד callback מודלי; נתיבי `chat` ו־
  `extract` רק שומרים תקציב ומחזירים `model_required` עם אפס שימוש בפועל.
- `rules.json` חוסם לפי עדיפות סיווג `loadChange` חסר או לא חוקי לפני כל
  כלל אחר; `rules.js` תומך ב־`not_in` דטרמיניסטי.
- `rebuildDerivedState` ו־`deriveCounters` דורשים `asOf` מפורש ואינם קוראים
  לשעון הקיר כברירת מחדל.
- `src/server.js` מחבר את `createStageTwoProcessor`, fixture ו־SQLite של
  שלב 2; אין חיבור למודל או לשיטס אמיתי.
- `.env.example`, `README.md` וגבול השגיאה של מתאם Sheets עודכנו לשלב 2.

**נבדק:**

- [x] סיווג עומס חסר, ריק או לא חוקי נכשל סגור.
- [x] מנוע שלב 2 שופט אימון דרך `rules.js` והמעבד המקומי מטפל בשאילתה.
- [x] נתיב מנותב־מודל אינו מפעיל callback ומדווח אפס שימוש בטוקנים.
- [x] `rebuild` והמונים דוחים קריאה ללא `asOf` מפורש.
- [x] `npm test`: 18 עברו, 0 נכשלו, כולל כל שבע רגרסיות שלב 1.

**נשבר / לא עובד:** אין כשל פתוח ידוע בשלב 2.

**PR:** תיקון `6281f7d` נדחף, תיאור PR מספר 2 עודכן וכל שלושת שרשורי
הביקורת סומנו כפתורים לאחר אימות התיקונים.

**פתוח לסשן הבא:** ביקורת חוזרת עצמאית על PR מספר 2. לאחר אישורה, לטפל
בשער `maxAttempts` לפני התחלת שלב 3.

**החלטות:** לא התקבלה החלטת ארכיטקטורה חדשה; התיקונים מיישמים את החוזים
הקיימים ולכן `DECISIONS.md` לא השתנה.

---

## 2026-08-16 — שלב 2 — ביקורת קוד PR 2 — Codex

**נבדק:**

- כל ה־diff של `agent/stage-2-engine` מול `main`, חוזי השכבות וכללי הבטיחות
  שב־`AGENTS.md`.
- `npm test`: 15 עברו, 0 נכשלו; `git diff --check` נקי וכל קובצי המקור
  נשארו מתחת ל־300 שורות.
- פורסמה ביקורת COMMENT על draft PR מספר 2 עם שלוש הערות inline.

**ממצאים:**

- P1 — `stage2.js` יכול להפעיל callback מודלי בלי להפעיל או לאכוף פסק של
  `rules.js`; מנוע הבטיחות נבדק רק בבידוד.
- P1 — כללי ה־hard block נכשלים פתוח אם `workout.loadChange` חסר או אינו
  בדיוק `increase`, גם כאשר כל נתוני היום־שאחרי חסרים.
- P2 — `rebuildDerivedState` ו־`deriveCounters` משתמשים כברירת מחדל בשעון
  הקיר, ולכן אותו מקור Sheets יכול להחזיר מצב שונה בתאריך UTC אחר.
- P2 — `npm start` עדיין מחבר את `createStageOneProcessor`, ולכן מנוע שלב 2
  אינו נגיש במסלול הריצה של היישום.

**שינויים בקוד:** לא בוצעו; זו הייתה ביקורת בלבד.

**פתוח לסשן הבא:** לתקן את ארבעת הממצאים בתוך גבולות שלב 2, להוסיף בדיקות
רגרסיה, להריץ את כל האימותים ולעדכן את PR מספר 2. אין להתחיל שלב 3.

**החלטות:** לא התקבלה החלטת ארכיטקטורה חדשה; `DECISIONS.md` לא השתנה.

---

## 2026-08-16 — שלב 2 — מנוע — Codex

**נבנה:**

- `src/router.js` — מנתב אירועים ל־`query`, `update`, `extract` או `chat`
  לפי תבניות בלבד, ללא גישה לכללים או לפסק.
- `src/rules.js` + `rules.json` — מנוע דטרמיניסטי שמריץ hard blocks לפי
  עדיפות ועוצר בחוסם הראשון; חסר נשמר כמצב נפרד מ־`clean`.
- `src/counters.js` + `src/rebuild.js` — מונים מפעילות שהושלמה ובנייה חוזרת
  של מצב נגזר דרך מתאם Sheets המקומי בלבד.
- `src/budget.js` + `src/stage2.js` — תקרת טוקנים קשיחה שנבדקת לפני גבול
  הפעולה המודלית, ושאילתת ימים מאז ריצה עם אפס טוקנים.
- `data/stage2-demo-sheets.json` — מקור דמה מקומי בלבד לבדיקות שלב 2.

**נבדק:**

- [x] "כמה ימים מאז ריצה" חוזר מיד עם אפס טוקנים וללא callback מודלי.
- [x] יום־אחרי חסר חוסם העלאת עומס.
- [x] ארבעה ניסיונות שכנוע עוינים אינם עוקפים hard block.
- [x] שתי הרצות `rebuild` מאותו fixture ובאותו `asOf` מחזירות מצב זהה.
- [x] מונים נגזרים מפעילות שהושלמה ומתעלמים מתוכנית ומטענת שיחה.
- [x] מיצוי התקציב חוסם פעולה מנותבת־מודל לפני callback.
- [x] כל שבע בדיקות הרגרסיה של שלב 1 ממשיכות לעבור.
- [x] `npm test`: 15 עברו, 0 נכשלו.

**נשבר / לא עובד:**

- בריצה הראשונה שתי בדיקות מונים נכשלו כי זיהוי `Run` היה רגיש לאותיות
  גדולות; ההתאמה תוקנה ל־case-insensitive וכל הבדיקות עברו לאחר מכן.
- אין כשל פתוח ידוע בשלב 2.
- הענף נדחף ו־draft PR מספר 2 עודכן עם מימוש שלב 2, גבולותיו ותוצאות האימות.

**פתוח לסשן הבא:** ביקורת חיצונית על PR מספר 2 וטיפול בממצאיה. אין להתחיל
שלב 3 לפני טיפול בסיכון poison-pill / `maxAttempts` ב־`db.js` לפי השער
המתועד.

**החלטות:** לא התקבלה החלטת ארכיטקטורה חדשה; `DECISIONS.md` לא השתנה.

---

## 2026-08-16 — הכנת תנאי פתיחה לשלב 2 — Codex

**הוכן:**

- נוסף `AGENTS.md` בשורש המאגר עם סדר הקריאה, גבולות ההרשאה וכללי העבודה
  המחייבים לכל שיחה חדשה.
- תחום שלב 2 הוגבל ל־`router.js`, מנוע כללים, `rules.json`, מונים, `rebuild`
  ותקרת תקציב — ללא AI, OCR, פרואקטיביות או חיבור לשיטס האמיתי.
- הוגדרו בדיקות היציאה, כולל ניסיונות שכנוע עוינים שאינם יכולים לעקוף hard
  block, `rebuild` דטרמיניסטי ואפס שימוש בטוקנים בשאילתות מקומיות.
- תועד שער שלב 3: טיפול ב־poison-pill / `maxAttempts` ב־`db.js` לפני תחילתו.

**נבדק:**

- [x] `main` היה נקי ומסונכרן לפני פתיחת ענף שלב 2.
- [x] כל שבע בדיקות שלב 1 עדיין עוברות.
- [x] לא נוסף קוד שלב 2 בסשן ההכנה.

**החלטות:** לא התקבלה החלטת ארכיטקטורה חדשה; `DECISIONS.md` לא השתנה.

**פתוח לסשן הבא:** לממש את שלב 2 בלבד ולהישאר על ענף
`agent/stage-2-engine` עד עמידה מלאה בקריטריוני היציאה וביקורת חיצונית.

---

## 2026-08-16 — שלב 1 — תיקוני ביקורת קוד חיצונית — Codex

**מקור:** שני ממצאי P1 מביקורת קוד חיצונית על PR 1. אלה תיקוני פגמים,
לא בקשות פיצ'ר.

**תוקן:**

- `src/queue.js` — תמונת אלבום שמגיעה לאחר שפריט האלבום יצא מ־`pending`
  אינה נעלמת עוד מהתור. היא מקבלת פריט עצמאי ומיידי עם מפתח
  `late:<mediaGroupId>:<updateId>` ופיילוד שמסמן `late: true` ואת
  `lateArrivalFor`. אין מיזוג אוטומטי עם הפריט שכבר בעיבוד או שהסתיים.
- `src/server.js` — כאשר `WEBHOOK_SECRET` ריק או חסר מחוץ לסביבת test,
  מודפסת ל־stderr אזהרת אבטחה בולטת לפני `server.listen()`; ההרצה אינה נחסמת.
- `.env.example` — מתעד במפורש שהשמטת `WEBHOOK_SECRET` משאירה את ה־webhook
  פתוח לחלוטין.

**נבדק:**

- [x] תמונה מאותו אלבום שמגיעה אחרי `claimNext()` יוצרת פריט תור שני.
- [x] התמונה המאוחרת קיימת גם ב־`raw_log` וגם בתור עם metadata מתאים.
- [x] כל שבע הבדיקות האוטומטיות עוברות: `7 passed`, `0 failed`.

---

## 2026-08-16 — שלב 1 — Codex

**נבנה:**

- `src/ingest.js` — רושם כל Telegram update לפני עיבוד המשך.
- `raw_log` — טבלת SQLite מסוג append-only עם `update_id` ייחודי.
- `src/queue.js` — תור סדרתי ועמיד עם retry ושחזור לאחר הפעלה מחדש.
- איחוד media album — ארבע תמונות באותה קבוצה הופכות לאירוע תור אחד.
- `src/webhook.js` — webhook מקומי תואם Telegram ונתיב health.
- `src/sheets.js` — קריאה מנתוני דמה מקומיים בלבד; כתיבה ועדכון חסומים.

**נבדק:**

- [x] עדכון כפול נשמר בלוג הגולמי אך אינו יוצר פריט תור כפול.
- [x] הלוג הגולמי נשאר עמיד גם אם ההכנסה לתור נכשלת.
- [x] ארבע תמונות באלבום יוצרות אירוע אחד לאחר חלון האיסוף.
- [x] פריט שהיה `processing` חוזר לעיבוד לאחר הפעלה מחדש.
- [x] ה־webhook מחזיר תשובה לאחר קליטה עמידה, בלי להמתין לעיבוד.
- [x] מתאם השיטס קורא נתוני דמה ודוחה כל ניסיון כתיבה.
- [x] כל שש הבדיקות האוטומטיות עוברות.

**גבול מפורש:**

- אין בקוד Google API client, פרטי התחברות או כתיבה לשיטס האמיתי.
- שלב 1 משתמש בנתוני דמה בלבד בהתאם להוראת המשתמש.
- `data/*.sqlite`, קובצי WAL/SHM ו־`.env` מוחרגים מ־Git.

**פתוח לסשן הבא:** שלב 2 — `router.js`, מנוע כללים, מונים, `rebuild`
ותקרת תקציב, עדיין ללא כתיבה לשיטס.

---

## 2026-08-16 — שלב 0 — נרמול סופי — Codex

**בוצע:**

- `Weight Tracker!A2:A27` — פורמט התצוגה של 26 תאריכים הומר מ־`D/M/YY`
  ל־`YYYY-MM-DD`; ערכי התאריך עצמם נשמרו.
- `Runs!E35:E37` — Goal Time רוקן בשלושת אימוני הכוח בתאריכים
  `2026-06-29`, `2026-07-15`, `2026-07-27`.

**נבדק:** כל 26 התאריכים תואמים ל־`YYYY-MM-DD`, ושלושת תאי Goal Time ריקים
ללא ערך מוזן. פורמט הזמן בתאים נשמר.

---

## 2026-08-16 — שלב 0 — השלמת פערים — Codex

**בוצע:**

- נוספו הכותרות החסרות ב־`Runs!AG2:AN2`, בלי לשנות את `AI2` הקיים.
- `Runs!AF46` עודכן בתגובת היום־אחרי של 14.08 תוך שמירת שאר תוכן התא.
- `Runs!AG46:AI46` מולאו `clean`, `clean`, `clean`.

**נבדק:** הכותרות, הטקסט וערכי התגובה נקראו מחדש ונמצאו תקינים.

---

## 2026-08-16 — שלב 0 — ניקוי נתונים — Codex

**בוצע:**

- `Runs!AI15` — נוסחת המעגל נוקתה ו־`#REF!` לא חזר.
- placeholders לא ישימים נוקו בשורות ובטווחים שאושרו ב־Runs.
- 15 ערכים לא רשמיים נוקו מ־`Splits!I`.
- השורה היתומה `Goals!A5:Z5` נוקתה.
- הכותרת `Next-Day Calf-Achilles` נוספה ל־`Runs!AI2`.

**נשמר במפורש:** `Runs!Y31` כבר היה ריק; `E30:G30`, `AF30`, ערכי כאבי הברך
ו־Elevation Gain של ריצת 29.07 לא שונו.

**הערה:** ההרשאה הייתה נקודתית בלבד ואינה מעניקה הרשאת כתיבה מתמשכת לשיטס.

---

## 2026-08-16 — שלב 0 — תכנון

**נבנה:** חבילת מפרט מלאה (8 מסמכים + 3 יומנים)

**החלטות:** DECISIONS.md #001–#012

**פתוח לסשן הבא:**
1. ניקוי 8 שגיאות השיטס (`02_SCHEMA.md`) — הושלם.
2. הוספת העמודות החדשות — הושלם.
3. רק אחר כך — שלב 1 — הושלם על נתוני דמה מקומיים.

שלב 0 נסגר לפני המשך הבנייה.

---

## היסטוריה

*(רשומות חדשות למעלה)*
