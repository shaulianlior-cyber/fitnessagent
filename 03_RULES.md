# 03 — מנוע הכללים

## הבעיה שזה פותר

**"כללים קשיחים שמתפתחים" הוא ניגוד פנימי:**
- כללים בקוד → לא מתפתחים בלי דיפלוי
- כללים בפרומפט → אפשר לשכנע אותם

## הפתרון

```
rules.json  ←  הכללים. דאטה.
rules.js    ←  המנוע. קוד. לא שואל שאלות.
```

**כלל חדש = שורה ב-JSON. אפס קוד. אפס דיפלוי.**

**והבוט לא יכול לרכך את עצמו** — הוא מציע רשומה, לא עורך את המנוע.

---

## פורמט כלל

```json
{
  "id": "next_day_required",
  "type": "hard_block",
  "priority": 10,
  "condition": {
    "field": "last_workout.next_day_knee",
    "op": "is_empty"
  },
  "action": "block_load_increase",
  "message_key": "next_day_missing",
  "severity": "blocking",
  "confidence": "high",
  "approved_date": "2026-08-11",
  "approved_by": "lior",
  "evidence": "...",
  "counter_examples": []
}
```

---

## סוגי כללים

| type | התנהגות |
|---|---|
| `hard_block` | חוסם. **לא ניתן לעקיפה בשום תנאי** |
| `soft_warn` | מתריע, לא חוסם |
| `counter` | מונה שנספר ומדווח |
| `pattern` | דפוס מאושר. מנחה ניסוח, לא חוסם |
| `pending` | מועמד. **לא מנחה החלטות** |

---

## סדר הרצה

```
1. hard_block לפי priority — עצירה בראשון שחוסם
2. soft_warn — נצברות
3. counters — מחושבים תמיד
4. patterns → coach.js
5. pending → לא נכנס לפרומפט ההחלטה
```

---

## כללי הליבה (נטענים בשלב 5)

| id | סוג | מה |
|---|---|---|
| `red_flag` | hard_block | כאב חד / נפיחות / נעילה / בריחה / החמרה → עצירה + פיזיו |
| `next_day_required` | hard_block | יום-אחרי ריק → אין העלאת עומס |
| `two_clean_runs` | hard_block | פחות משתי תגובות נקיות רצופות → אין העלאה |
| `lateral_load` | soft_warn | עומס לטרלי מצטבר |
| `pull_push_ratio` | counter | יעד 2:1 |
| `days_since_run` | counter | |
| `days_since_knee_routine` | counter | **ציות, לא דיווח** |
| `days_since_weigh_in` | counter | |

---

## מונים — למה הם קריטיים

**מונה סופר מה בוצע. דיווח סופר מה נאמר.**

במערכת הקודמת שגרת הברך לא בוצעה ארבעה חודשים והמערכת דיווחה "ברך ירוקה" בכל שיחה — כי לא היה מונה.

מונים מחושבים בקוד מהשיטס, לא ע"י המודל.

---

## שינוי כלל — שער האישור

```
הצעה (חקירה/משתמש)
   ↓
סריקת דוגמאות-נגד ← קוד, כל ההיסטוריה. 3 סתירות = חסימה
   ↓
מנתח שני ← נתונים גולמיים בלבד. בלי המסקנה, בלי הדפוסים המאושרים
   ↓
הצגה למשתמש: ראיות · ביטחון · דוגמאות-נגד · דעת המנתח השני
   ↓
אישור מפורש
   ↓
רשומה חדשה + הישנה מקבלת superseded_by
```

**הכלל הישן לא נמחק אף פעם.** יש היסטוריה מלאה.

**הבוט לא משנה כלל בעצמו. אף פעם.**

---

## על המנתח השני

**עצמאות אמיתית דורשת קונטקסט שונה, לא רק מודל שונה.** שני מודלים שקוראים את אותם דפוסים מאושרים יטו לאותה מסקנה.

לכן המנתח השני מקבל:
- ✅ נתונים גולמיים
- ❌ המסקנה
- ❌ הנימוק
- ❌ הדפוסים המאושרים

**תקדים:** במערכת הקודמת המנתח השני זיהה שישה גורמים מתחרים שלא ניתן להפריד, במקום מועמד אחד עם ביטחון גבוה מדי. הביטחון ירד מ'בינוני' ל'נמוך'. זה עבד.
